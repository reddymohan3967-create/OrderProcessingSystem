This directory contains the repository-local SQLite database `orders.db` used by the services for development and demos.

Notes:
- This DB is intentionally committed to the repository for convenience. It may contain example data.
- If you prefer to keep the DB out of source control, add `data/orders.db` to `.gitignore` and remove the file from the repo.

Do not store production or sensitive data in this file.